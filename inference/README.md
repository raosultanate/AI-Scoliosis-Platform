# Inference

Deferred until an evaluated landmark detector exists. Future inference should emit the canonical
landmark domain contract consumed by the existing geometry pipeline. The current API only runs the
bundled synthetic landmark case and reports that real-image inference is unavailable.
